Working notes (internal): User requested removal of Helper folder and ApiHelper integrations in MVC client controllers. Will search for ApiHelper usages and edit controllers to remove dependency. Need to ensure sign-in requires access to website. Likely change: use cookie authentication and Authorize attributes, and perform login via AuthController? But user likely wants to remove helpers that call API and instead use server-side calls or simply restrict access. Ask clarifying question before making destructive edits.